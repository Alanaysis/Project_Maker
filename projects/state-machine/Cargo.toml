[package]
name = "state-machine"
version = "0.1.0"
edition = "2021"
description = "A generic state machine framework supporting state transitions, event-driven, and history recording"
license = "MIT"

[dependencies]
thiserror = "1.0"
log = "0.4"

[dev-dependencies]
env_logger = "0.10"

# 简单流水线示例 - 用于快速测试

name: "简单测试流水线"

stages:
  - name: build
    tasks:
      - name: "构建"
        command: "echo '构建完成'"
        timeout: 30

  - name: test
    depends_on:
      - build
    tasks:
      - name: "测试"
        command: "echo '测试通过'"
        timeout: 30

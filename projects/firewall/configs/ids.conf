# 入侵检测系统配置

# SYN Flood 检测阈值（包/秒）
syn_flood_threshold = 100

# 端口扫描检测阈值（端口/分钟）
port_scan_threshold = 20

# 最小包大小（字节）
pkt_size_min = 20

# 最大包大小（字节）
pkt_size_max = 1500

# ICMP Flood 阈值（包/秒）
icmp_flood_threshold = 50

# UDP Flood 阈值（包/秒）
udp_flood_threshold = 1000
